import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hu.kr8.yourney',
  appName: 'Yourney',
  webDir: 'build',
  server: {
    androidScheme: 'http'
  },
};

export default config;
